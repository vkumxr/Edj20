I was asked to temporarily hold the current dashboard development work and instead focus on understanding how to build a proper Windows desktop application.

The preferred approach is to use C# with Microsoft Visual Studio Community instead of VS Code.

Along with that, I was asked to explore how communication between edge devices can be established using TCP/IP. The goal is to understand how devices connect, send data, and receive data over the network, which will later be useful for communication with the EDJ20 and related systems.

So currently, my focus is:

* learning Windows application development using C#,
* understanding how TCP/IP communication works,
* testing communication between devices,
* and figuring out how edge devices exchange data over a network.
